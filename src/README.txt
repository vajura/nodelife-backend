Please make sure the pub-key is converted to RFC4716 format with 'ssh-keygen -e -f ~/.ssh/id_rsa.pub'!

Write the complete output into the file:

~/.ssh/authorized_keys

--

All files older than 30 days will be deleted!
